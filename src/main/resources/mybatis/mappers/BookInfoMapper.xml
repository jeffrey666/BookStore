<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.tarena.book.mapper.BookInfoMapper">

    <select id="findAllUser" resultType="cn.tarena.book.pojo.User">
        select * from user
    </select>
    
    <insert id="saveBookUpload" >
    insert into book_info (book_info_id,author,imgurl,pub_date,category) 
    		values(#{bookInfoId},#{author},#{imgurl},#{pubDate},#{category})
    </insert>

</mapper>